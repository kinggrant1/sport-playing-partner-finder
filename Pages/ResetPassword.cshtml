@* @page
@model MyWebApp.Pages.ResetPasswordModel
@{
    ViewData["Title"] = "Reset Password";
}
@{
    var successMessage = TempData["SuccessMessage"] as string;
}

@if (!string.IsNullOrEmpty(successMessage))
{
    <script>
        window.onload = function() {
            // show the success message for login/signup
            alert("@successMessage");
        };
    </script>
}

<head>
    <link rel="stylesheet" href="~/css/Login.css">
</head>
<h2>Reset Password</h2>

@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <div class="alert alert-danger">@Model.ErrorMessage</div>
}

<form method="post">
    <input type="hidden" asp-for="email" />
    <div>
        <label asp-for="ResetCode">Reset Code</label>
        <input asp-for="ResetCode" required />
    </div>
    <div>
        <label asp-for="NewPassword">New Password</label>
        <input asp-for="NewPassword" type="password" required />
    </div>
    <button type="submit">Reset Password</button>
</form> *@
@page
@model MyWebApp.Pages.ResetPasswordModel
@{
    ViewData["Title"] = "Reset Password";
}
@{
    var successMessage = TempData["SuccessMessage"] as string;
}

@if (!string.IsNullOrEmpty(successMessage))
{
    <script>
        window.onload = function() {
            // show the success message for login/signup
            alert("@successMessage");
        };
    </script>
}

<head>
    <link rel="stylesheet" href="~/css/Login.css">
</head>
<h2>Reset Password</h2>

<form method="post">
    <div>
        <label asp-for="ResetCode">Reset Code</label>
        <input asp-for="ResetCode" required />
    </div>
    <div>
        <label asp-for="NewPassword">New Password</label>
        <input asp-for="NewPassword" type="password" required />
    </div>
    <button type="submit">Reset Password</button>
</form>

